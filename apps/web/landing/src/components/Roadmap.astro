---
import Section from "./Section.astro";

/**
 * What is deliberately missing, stated as what comes next. Every item here is
 * absent from the current release; none of it is implied to exist.
 */

const items = [
  {
    title: "Sign in with Google",
    body: "Identity already sits behind a provider interface, and the database column is plain text. Adding one is a file and two secrets, not a migration",
  },
  {
    title: "Processes that outlive the connection",
    body: "start_command runs a dev server or a watch task today, but it dies when the CLI disconnects. Keeping one alive across a dropped socket means deciding what happens to output nobody read, which is a different question",
  },
  {
    title: "Confirmation inside the client",
    body: "Approving a change works on every client: in the conversation on MCP 2026-07-28, and otherwise on the machine's terminal or in the dashboard. Claude and ChatGPT still speak the 2025 protocol, so today they take the second path",
  },
  {
    title: "Billing and plans",
    body: "There are none. Nothing is metered, nothing is charged, and no limit exists beyond the rate limiting that stops a caller hammering the gateway",
  },
];
---

<Section
  id="roadmap"
  eyebrow="Roadmap"
  title="What comes next"
  subtitle="Named here because they are not in this release, and it is better to say so than to let a demo imply otherwise"
>
  <div class="grid gap-4 sm:grid-cols-2">
    {
      items.map((item, index) => (
        <article
          class="animate-fade-in-up opacity-0-initial border-border bg-surface flex gap-4 rounded-xl border p-5"
          style={`animation-delay: ${120 + index * 70}ms`}
        >
          <span
            aria-hidden="true"
            class="bg-accent-subtle text-foreground-faint mt-0.5 flex size-7 shrink-0 items-center justify-center rounded-md"
          >
            <svg
              viewBox="0 0 24 24"
              class="size-3.5"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              aria-hidden="true"
            >
              <path d="M12 5v14M5 12h14" />
            </svg>
          </span>
          <div>
            <h3 class="text-title-lg">{item.title}</h3>
            <p class="text-body-md text-foreground-muted mt-1.5">{item.body}</p>
          </div>
        </article>
      ))
    }
  </div>
</Section>
